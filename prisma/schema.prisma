// Minimal schema to satisfy Shopify CLI
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.sqlite"
}

// Empty - we don't actually use Prisma
model Dummy {
  id Int @id @default(autoincrement())
}